<!DOCTYPE html>
<html>
<head>
  <!-- Standard Meta -->
  <meta charset="utf-8" />
  <meta http-equiv="X-UA-Compatible" content="IE=edge,chrome=1" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0, maximum-scale=1.0">

  <!-- Site Properties -->
  <title>Login</title>

</head>
<body>

  <div class="ui center aligned page grid">
    <div class="ui column">
      <div class="content">
      </div>
        <%= form_tag signin_path, {class: "ui huge form"} do %>
          <div class="ui raised padded black segment">
            <h1 class="ui black image header">
              Tekcít
            </h1><br />
            <label><h3><b>Username:</b></h3></label><br>
            <div class="field">
              <div class="ui left icon fluid input">
                <i class="user icon"></i>
                  <%= text_field_tag 'user[username]', "steve.taylor", required: true %><br>
              </div>
            </div>
          <label><h3><b>Password:</b></h3></label><br>
          <div class="field">
            <div class="ui left icon fluid input">
              <i class="lock icon"></i>
                <%= password_field_tag 'user[password]', "helloworld!", required: true %><br>
            </div>
          </div>
          <%= submit_tag "Sign In", class: "ui fluid large black submit button" %>
        <% end %>
        <div class="ui message">
          New to us? <a href="<%= signup_path %>">Sign Up</a>
        </div>
      </div>
    </div>
  </div>

</body>

</html>
